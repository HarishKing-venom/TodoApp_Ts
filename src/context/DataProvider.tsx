{/*import { StyleSheet, Text, View } from 'react-native';
import React, { createContext, useContext, useEffect, useState, useLayoutEffect } from 'react';
import AsyncStorage from '@react-native-async-storage/async-storage';

const NoteContext = createContext();
const NoteProvider = () => {
    const [alarmcollection, setAlarmCollection] = useState([]);
    const [dateinfo, setdateinfo] = useState();
    const [hoursinfo, sethoursinfo] = useState();
    const [minutesinfo, setminutesinfo] = useState();
    const [titleinfo, settitleinfo] = useState();
    const [descriptioninfo, setDescriptioninfo] = useState();
  
       
  return (
    <NoteContext.Provider value={{ alarmcollection, setAlarmCollection, dateinfo, setdateinfo, hoursinfo, sethoursinfo, minutesinfo, setminutesinfo, titleinfo, settitleinfo, descriptioninfo, setDescriptioninfo }}>
        {children}
    </NoteContext.Provider>
  )
}

export const useNotes = () =>useContext(NoteContext);

export default NoteProvider;*/}